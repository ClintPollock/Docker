# Britive Broker Configuration Template
# Copy this file to .env and fill in your values

# Required: Britive tenant configuration
TENANT_SUBDOMAIN=your-tenant-name
AUTHENTICATION_TOKEN=your-auth-token

# Optional: Java memory settings
JAVA_OPTS=-Xmx512m -Xms256m

# SSH Keys Configuration
# Path to directory containing your .pem files on the host system
# Examples:
#   Linux/Mac: SSH_KEYS_HOST_PATH=/home/user/keys
#   Windows: SSH_KEYS_HOST_PATH=C:\Users\username\keys
#   Relative to docker-compose.yml: SSH_KEYS_HOST_PATH=./ssh-keys
SSH_KEYS_HOST_PATH=./ssh-keys

# Default SSH key filename (without path, just the filename)
# This will be looked for in the SSH_KEYS_HOST_PATH directory
DEFAULT_SSH_KEY=your-key.pem